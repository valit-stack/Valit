language: csharp
os:
  - osx
  - linux

sudo: required
dist: trusty
addons:
  apt:
    packages:
    - gettext
    - libcurl4-openssl-dev
    - libicu-dev
    - libssl-dev
    - libunwind8

osx_image: xcode8.3

mono:
  - 4.4.2

dotnet: 2.0.0

script:
  - ./build.sh 

cache:
  directories:
    - .packages
    - tools/Addins